// import User from '../../../models/user.js';

export const getUser = async (req, res) => {
  try {
    // const user = await User.findAll({
    //   at,
    // });
  } catch (error) {
    console.log(error);
  }
};

export const getUserOne = async () => {
  try {
    console.log('테이블 선택 정보');
  } catch (error) {
    console.log(error);
  }
};
